#include <iostream>
#include <math.h>
using namespace std;    // https://memepedia.ru/wp-content/uploads/2018/08/wo6cxxstlzc-768x597.jpg

int main()
{
	double a, b, c, x;
	setlocale(LC_ALL, "Rus");
	cin >> a >> b >> c;

	if ((a == 0) && (b == 0))
	{
		cout << "Уравнение введено неверно\n";
		return 1;
	}

	if ((b == 0) && (c == 0)||((a==0)&&(c==0)))
	{
		cout << "X=0\n";
		return 0;
	}

	if (a == 0)
	{
		cout << "x= " << -c / b;
	}
	else
	{

		c = sqrt(b * b - 4 * a * c);
		if (c >= 0)
		{
			x = ((-b + c) / (2 * a));
			a = ((-b - c) / (2 * a));
			
			if (x==a)
			{
				cout << "X = " << x;
			}
			else
			{
				cout << "X1 = " << x << "\n";
				cout << "X1 = " << a << "\n";
			}

		}
		else
			cout << "нет вещественных решений";
	}
	cout << "\n";
}   
